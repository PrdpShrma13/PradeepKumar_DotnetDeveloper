@model Pradeep_DotnetDeveloper.Models.StudentListViewModel

@{
    ViewBag.Title = "GetStudents";
}

<h2 class="text-center">Students</h2>

<div style="margin-top:30px;">
    <div></div>
    <div></div>
    <div class="pull-right">
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#addStudentModal">
            Add Student
        </button>
    </div>
</div>

<div>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Class</th>
                <th>Subject</th>
                <th>Marks</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Students)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.FirstName</td>
                    <td>@item.LastName</td>
                    <td>@item.Class</td>
                    <td></td>
                    <td></td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Modal -->
<div class="modal fade" id="addStudentModal" tabindex="-1" role="dialog" aria-labelledby="addStudentModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addStudentModalLabel">Add Student</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body" style="min-height:400px">
                <form id="studentForm">
                    <div class="col-md-12" style="margin:10px">
                        <div class="col-md-5">
                            <label for="firstName">First Name</label>
                        </div>
                        <div class="col-md-5">
                            <input type="text" name="firstName" id="firstName" placeholder="First Name" />
                        </div>
                    </div>
                    <div class="col-md-12" style="margin:10px">
                        <div class="col-md-5">
                            <label for="lastName">Last Name</label>
                        </div>
                        <div class="col-md-5">
                            <input type="text" name="lastName" id="lastName" placeholder="Last Name" />
                        </div>
                    </div>
                    <div class="col-md-12" style="margin:10px">
                        <div class="col-md-5">
                            <label for="standerd">Class</label>
                        </div>
                        <div class="col-md-5">
                            <input type="text" name="standerd" id="standerd" placeholder="Class" />
                        </div>
                    </div>
                    <div class="col-md-12" style="margin:10px">
                        <div class="col-md-5">
                            <label for="subject">Subjects</label>
                        </div>
                        <div class="col-md-5">
                            <label for="marrks">Marks</label>
                        </div>
                    </div>
                    <div class="col-md-12" style="margin:10px">
                        @foreach (var item in Model.Subjects)
                        {
                            <div class="col-md-5">
                                <input type="text" id="@item.Id-@item.SubjectName" placeholder="@item.SubjectName" disabled />
                            </div>
                            <div class="col-md-5">
                                <input type="number" id="@item.SubjectName.ToLower()marks" placeholder="Marks" />
                            </div>
                        }
                    </div>
                </form>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <button type="button" class="btn btn-primary" onclick="SaveStudent()">Save changes</button>
        </div>
    </div>
</div>

<script>
    function SaveStudent() {
        var firstName = document.getElementById("firstName").value;
        if (firstName.length < 1) {
            alert("Please Enter First Name");
            return;
        }

        var lastName = document.getElementById("lastName").value;
        if (lastName.length < 1) {
            alert("Please Enter Last Name");
            return;
        }

        var standerd = document.getElementById("standerd").value;
        if (standerd.length < 1) {
            alert("Please Enter Class");
            return;
        }
        var marks = [];
        if (document.getElementById("mathmarks").value.length > 0) {
            var marksArray = [{
                subjectId: 1,
                marks: parseInt(document.getElementById("mathmarks").value)
            }]
            marks.push(marksArray);
        }
        if (document.getElementById("physicsmarks").value.length > 0) {
            var marksArray = [{
                subjectId: 2,
                marks: parseInt(document.getElementById("physicsmarks").value)
            }]
            marks.push(marksArray);
        }
        if (document.getElementById("chemistrymarks").value.length > 0) {
            var marksArray = [{
                subjectId: 3,
                marks: parseInt(document.getElementById("chemistrymarks").value)
            }]
            marks.push(marksArray);
        }
        debugger
        $.post("/Home/PostStudent",
            {
                firstName: firstName,
                lastName: lastName,
                class: standerd,
                StudentMarks: marks
            },
            function (data) {
               
            });
    }


</script>